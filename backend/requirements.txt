# Exactly the stack frozen in CLAUDE.md §3. No additions without a line in docs/SPEC.md §4.
fastapi==0.115.*
uvicorn[standard]==0.32.*
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pydantic-settings==2.6.*
python-multipart==0.0.*

# Image quality + perceptual-hash dedupe (SPEC §12)
imagehash==4.3.*
pillow==11.*

# Detection only — never people, vehicles or licence plates (CLAUDE.md §2.4)
ultralytics==8.3.*

# Tooling
ruff==0.16.*
pytest==8.3.*
httpx==0.27.*
